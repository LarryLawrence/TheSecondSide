<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drunkpiano.thesecondside">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- //信Activity -->
        <activity android:name=".LetterActivity">

            <!-- 没BAR,黑色主题android:theme="@style/Theme.AppCompat.NoActionBar" -->
            <!-- 没BAR,白色主题android:theme="@style/Theme.AppCompat.Light.NoActionBar" -->
            <!-- 没BAR,白色主题android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" -->
        </activity>
        <activity android:name=".RealLetterActivity"></activity>
    </application>

</manifest>